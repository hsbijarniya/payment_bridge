name: payment_bridge
description: All-in-one payment gateway implementation of Razorpay, Paytm etc.
version: 0.0.1
homepage: https://ensorta.com
repository: https://github.com/hsbijarniya/payment_bridge

environment:
  sdk: ">=2.16.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  nb_utils: ^4.4.10
  razorpay_flutter: ^1.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
